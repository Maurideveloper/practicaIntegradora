<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Products</title>
</head>

<body>
    <h1 class="title_products">Productos disponibles</h1>
    {{#each allProducts}}
    <ul class="product_box">
        <li class="product_list">
            <h2>{{title}}</h2>
            <p>Descripción: {{description}}</p>
            <p>Precio: ${{price}}</p>
            <p>Codigo Prod.: {{code}}</p>
            <p>Stock: {{stock}}</p>
        </li>
    </ul>
    {{/each}}
</body>

</html>